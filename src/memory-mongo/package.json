{
  "name": "@berwickgeek/mcp-memory-mongo",
  "version": "0.1.0",
  "description": "MCP server for enabling memory for Claude through a knowledge graph with MongoDB API persistence",
  "license": "MIT",
  "author": "Jay Berwick",
  "homepage": "https://github.com/berwickgeek/mcp-servers",
  "bugs": "https://github.com/berwickgeek/mcp-servers/issues",
  "type": "module",
  "bin": {
    "mcp-memory-mongo": "dist/index.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc && shx chmod +x dist/*.js",
    "prepare": "npm run build",
    "watch": "tsc --watch"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.0.1",
    "axios": "^1.6.2"
  },
  "devDependencies": {
    "@types/node": "^22.9.3",
    "shx": "^0.3.4",
    "typescript": "^5.6.2"
  }
}
